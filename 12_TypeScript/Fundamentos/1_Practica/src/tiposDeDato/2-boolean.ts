// Tipado explícito
let isPro: boolean;
isPro = true;

// Si asignamos un tipo number a uno boolean me marca error
// isPro = 5;

// Inferido
let isUserPro = false;
isUserPro = true;

// Si asignamos un tipo number a uno boolean me marca error
// isUserPro = 54;